# possible values : production, development
app_mode = development

[SERVER]
origin = "http://localhost:3000", "http://localhost:3333"

[DATABASE]
host = localhost

user = pbg

dbname = pbgcare

password = prudent

http_port = 8080

driver = postgres 

sslmode = disable 

dsn = host=%(host)s user=%(user)s dbname=%(dbname)s sslmode=%(sslmode)s password=%(password)s


[paths]
# Path to where grafana can store temp files, sessions, and the sqlite3 db (if that is used)
data = /tmp